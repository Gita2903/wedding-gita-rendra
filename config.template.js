// Template ini di-generate jadi config.js pas deploy, jangan diisi manual di sini.
// Isi nilainya lewat environment variable SUPABASE_URL & SUPABASE_ANON_KEY.
window.APP_CONFIG = {
  SUPABASE_URL: "${SUPABASE_URL}",
  SUPABASE_ANON_KEY: "${SUPABASE_ANON_KEY}"
};
